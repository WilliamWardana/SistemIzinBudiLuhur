<div class="overlay"></div>
<div class="container container-custom">
  <h1>Status anda adalah?</h1>
  <div class="row justify-content-center g-4">
    <div class="col-md-10 col-12">
      <a href="#" class="text-decoration-none popup-link-siswa">
        <div class="glass-card">
          <h3 class="text-success fw-bold">👨‍🎓 Saya Siswa</h3>
          <p class="text-light">Masuk untuk mengajukan izin</p>
        </div>
      </a>
    </div>

    <div class="col-md-10 col-12">
      <a href="#" class="text-decoration-none popup-link-guru">
        <div class="glass-card">
          <h3 class="text-info fw-bold">🏫 Saya Warga Sekolah</h3>
          <p class="text-light">Masuk untuk memverifikasi izin</p>
        </div>
      </a>
    </div>
  </div>
</div>

<div class="modal fade" id="siswaModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content rounded-4 shadow">
      <form method="post" action="/murid_form">
        <div class="modal-header bg-success text-white rounded-top-4">
          <h5 class="modal-title">Masukkan NIS</h5>
          <button
            type="button"
            class="btn-close btn-close-white"
            data-bs-dismiss="modal"
          ></button>
        </div>
        <div class="modal-body">
          <input
            type="number"
            class="form-control"
            name="nis"
            placeholder="Masukkan NIS"
            required
            autocomplete="off"
          />
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-glass" data-bs-dismiss="modal">
            Batal
          </button>
          <button type="submit" class="btn btn-glass">Submit</button>
        </div>
      </form>
    </div>
  </div>
</div>

<div class="modal fade" id="guruModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content rounded-4 shadow">
      <form method="post" action="/guru_form">
        <div class="modal-header bg-primary text-white rounded-top-4">
          <h5 class="modal-title">Masukkan Password</h5>
          <button
            type="button"
            class="btn-close btn-close-white"
            data-bs-dismiss="modal"
          ></button>
        </div>
        <div class="modal-body">
          <input
            type="password"
            class="form-control"
            name="password"
            placeholder="Masukkan Password"
            required
          />
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-glass" data-bs-dismiss="modal">
            Batal
          </button>
          <button type="submit" class="btn btn-glass">Submit</button>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
  body {
    height: 100vh;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    background: url("/images/Perpus.jpg") no-repeat center center/cover;
    overflow-y: hidden;
  }

  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
  }

  .container-custom {
    position: relative;
    z-index: 1;
    text-align: center;
    color: white;
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 1rem;
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  @media (min-width: 768px) {
    .glass-card:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
    }
  }

  h1 {
    font-weight: bold;
    margin-bottom: 2rem;
  }

  .modal-content {
    background: rgba(255, 255, 255, 0.1) !important;
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
    border-radius: 15px;
    overflow: hidden;
  }

  .modal-header {
    background: transparent !important;
    border-bottom: none;
    color: white;
  }

  .modal-footer {
    border-top: none;
    background: transparent;
  }

  .modal-content .form-control {
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: white;
  }

  .modal-content .form-control::placeholder {
    color: rgba(255, 255, 255, 0.7);
  }

  .btn-glass {
    background: rgba(255, 255, 255, 0.15);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(5px);
    transition: all 0.3s ease;
  }

  .btn-glass:hover {
    background: rgba(255, 255, 255, 0.3);
    color: #000;
  }
</style>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
  document
    .querySelector(".popup-link-siswa")
    .addEventListener("click", function (e) {
      e.preventDefault();
      new bootstrap.Modal(document.getElementById("siswaModal")).show();
    });

  document
    .querySelector(".popup-link-guru")
    .addEventListener("click", function (e) {
      e.preventDefault();
      new bootstrap.Modal(document.getElementById("guruModal")).show();
    });
</script>
